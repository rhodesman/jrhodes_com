module.exports = function (app) {
  // The React app is served by express.static(site/dist) in app.js, which
  // handles `/` automatically via index.html. Route definitions are kept
  // here for any future server-side endpoints (e.g. contact form, API).
  void app;
};
